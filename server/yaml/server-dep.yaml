apiVersion: v1
kind: Service
metadata:
  name: connector-server
spec:
  selector:
    app: connector-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: conector-server
  labels:
    app: conector-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: conector-server
  template:
    metadata:
      labels:
        app: conector-server
    spec:
      containers:
        - name: conector-server
          image: quay.io/bryonbaker/connector-server:latest
          ports:
            - containerPort: 8080